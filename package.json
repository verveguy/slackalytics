{
  "name": "SlackGAIntegration",
  "version": "1.0.0",
  "description": "Integration with GA to track Slack usage",
  "main": "app.js",
  "dependencies": {
    "request": "^2.54.0",
    "querystring": "^0.2.0",
    "claudia-api-builder": "^1.6.0",
    "aws-sdk": "^2.4.11",
    "request-promise-native" : "^1.0.3"
  },
  "devDependencies": {
    "claudia": "^1.6.2",
    "ntl": "^1.1.3"
  },
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "create": "claudia create --region us-east-1 --allow-recursion --api-module app",
    "deploy": "claudia update",
    "destroy": "claudia destroy",
    "start": "ntl"
  },
  "files": [ "*.js" ]
}
